---
---
<section class="relative min-h-[90vh] flex items-center justify-center overflow-hidden bg-slate-900 border-b border-slate-800">
  <!-- Decorative background elements -->
  <div class="absolute inset-0 z-0">
    <div class="absolute -top-40 -left-40 w-[800px] h-[800px] bg-primary/10 rounded-full blur-[120px] mix-blend-screen opacity-50 animate-pulse"></div>
    <div class="absolute -bottom-40 -right-40 w-[600px] h-[600px] bg-success-eco/10 rounded-full blur-[100px] mix-blend-screen opacity-50 animate-pulse" style="animation-delay: 2s"></div>
    <!-- Grid Pattern -->
    <div class="absolute inset-0 bg-[url('/grid-pattern.svg')] opacity-5"></div>
  </div>

  <div class="container mx-auto px-6 relative z-10 grid lg:grid-cols-2 gap-16 items-center">
    <div class="text-left space-y-8 max-w-2xl">
      <div class="inline-flex items-center gap-2 px-3 py-1 rounded-full bg-slate-800/50 border border-slate-700 backdrop-blur-sm shadow-lg mb-4 animate-fade-in-up">
        <span class="w-2 h-2 bg-success-eco rounded-full animate-ping"></span>
        <span class="text-xs font-semibold text-slate-300 uppercase tracking-widest">NCSC Approved ITAD</span>
      </div>
      
      <h1 class="text-5xl lg:text-7xl font-bold text-white leading-[1.1] animate-fade-in-up delay-100">
        Secure Data <br />
        <span class="text-transparent bg-clip-text bg-gradient-to-r from-primary to-blue-400">Destruction</span> & 
        <span class="italic font-light text-slate-400">Recycling</span>
      </h1>
      
      <p class="text-xl text-slate-400 leading-relaxed font-light animate-fade-in-up delay-200">
        Antares Systems provides fully accredited IT Asset Disposal services for enterprise and public sector organizations across London and the UK.
      </p>
      
      <div class="flex flex-col sm:flex-row gap-4 pt-4 animate-fade-in-up delay-300">
        <a href="/contact" class="btn-primary flex items-center justify-center gap-2 px-8 py-4 text-lg">
          Get a Secure Quote
          <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3"></path></svg>
        </a>
        <a href="/services/data-destruction" class="px-8 py-4 rounded-lg bg-white/5 hover:bg-white/10 border border-white/10 text-white font-semibold transition-all backdrop-blur-sm flex items-center justify-center gap-2">
          View Standards
        </a>
      </div>
      
      <div class="pt-8 flex items-center gap-8 border-t border-white/5 animate-fade-in-up delay-500">
        <div class="flex flex-col">
          <span class="text-2xl font-bold text-white">100%</span>
          <span class="text-xs text-slate-500 uppercase">Data Wipe Guarantee</span>
        </div>
        <div class="w-px h-8 bg-white/10"></div>
        <div class="flex flex-col">
          <span class="text-2xl font-bold text-white">0%</span>
          <span class="text-xs text-slate-500 uppercase">Landfill Policy</span>
        </div>
        <div class="w-px h-8 bg-white/10"></div>
        <div class="flex flex-col">
          <span class="text-2xl font-bold text-white">24h</span>
          <span class="text-xs text-slate-500 uppercase">Service Response</span>
        </div>
      </div>
    </div>
    
    <!-- Hero Visual / Form Placeholder -->
    <div class="hidden lg:block relative animate-fade-in delay-500">
      <div class="absolute -inset-1 bg-gradient-to-r from-primary to-success-eco rounded-2xl blur opacity-20"></div>
      <div class="relative bg-slate-900/90 backdrop-blur-xl border border-slate-700 p-2 rounded-2xl shadow-2xl transform rotate-1 hover:rotate-0 transition-transform duration-500">
         <img src="/F3.png" alt="Secure Data Destruction" class="rounded-xl w-full h-auto object-cover opacity-90 hover:opacity-100 transition-opacity dragger" />
         
         <!-- Floating Badge -->
         <div class="absolute -bottom-6 -left-6 bg-white p-4 rounded-lg shadow-xl border border-slate-100 flex items-center gap-4 animate-bounce-slow">
            <div class="bg-success-eco/10 p-3 rounded-full text-success-eco">
               <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path></svg>
            </div>
            <div>
               <p class="text-xs font-bold text-slate-400 uppercase">Compliance</p>
               <p class="text-sm font-bold text-slate-900">GDPR & ISO 27001</p>
            </div>
         </div>
      </div>
    </div>
  </div>
</section>

<style>
  @keyframes fade-in-up {
    from { opacity: 0; transform: translateY(20px); }
    to { opacity: 1; transform: translateY(0); }
  }
  .animate-fade-in-up {
    animation: fade-in-up 0.8s ease-out forwards;
    opacity: 0;
  }
  .delay-100 { animation-delay: 0.1s; }
  .delay-200 { animation-delay: 0.2s; }
  .delay-300 { animation-delay: 0.3s; }
  .delay-500 { animation-delay: 0.5s; }
  
  @keyframes bounce-slow {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(-10px); }
  }
  .animate-bounce-slow {
    animation: bounce-slow 3s infinite ease-in-out;
  }
</style>
